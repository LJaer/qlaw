<view class="q-types">
  <navigator class="q-type q-list-item"
              url="../laws/laws?type={{item.id}}"
              wx:for="{{lawTypes}}"
              wx:key="id">{{item.name}}</navigator>
</view>